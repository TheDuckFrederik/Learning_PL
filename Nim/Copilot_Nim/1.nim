# 1.
echo("Hi Mom!");
# 2.
var x = 1;
const y = 2;
echo(x, "\n", y);
# 3.
var sum = x + y;
var sub = x - y;
var mult = x * y;
var divi = x / y;
echo(sum, "\n", sub, "\n", mult, "\n", divi);
# 4.
if x mod 2 == 0:
  echo("x is even");
else:
  echo("x is odd");
if y mod 2 == 0:
  echo("y is even");
else:
  echo("y is odd");
# 5.
for i in countdown(10, 1):
  echo(i)
# 6.
var arr = [1, 2, 3, 4, 5];
for i in arr:
  echo(i);
# 7.
var name = readLine(stdin);
echo("Hello, ", name, "!");
# 8.
var h = "Hi";
var m = "Mom";
echo(h, " ", m, "!");
# 9.
var name = readLine(stdin);
echo("Hello, ", name, "!");
# 10.

